# Resend API key — create one at https://resend.com/api-keys
# You also need to verify the revenueinstitute.com domain in Resend
RESEND_API_KEY=

# Exa API key — create one at https://dashboard.exa.ai/api-keys
# Powers assessment company enrichment (industry/size/location from a
# prospect's website). Optional: if unset, assessments still work but skip
# the personalized "what others in your industry are doing" section and
# fall back to a generic, unconfirmed-industry version of it.
EXA_API_KEY=